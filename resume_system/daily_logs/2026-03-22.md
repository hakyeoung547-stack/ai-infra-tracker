# Daily Log — 2026-03-22

## 오늘 한 일
- 파이썬 async/await 전체 구조 정리 — 코루틴 개념부터 create_task까지
- `async def` 호출이 실행이 아니라 코루틴 객체 생성임을 직접 확인
- `await` 위치 규칙 정리 (async 안 → await / 바깥 → asyncio.run())
- `time.sleep` vs `asyncio.sleep` 차이 이해 — await 없이 쓰면 실제로 안 쉼
- `asyncio.gather` vs `asyncio.create_task` 차이 체득
- async가 유리한 경우(I/O) vs 아닌 경우(CPU 계산) 구분 정리
- 학습 내용을 blog draft + Jupyter Notebook(06_async_await.ipynb)으로 재구성

## 사용한 기술 / 도구
- Python (asyncio, async/await, coroutine, gather, create_task)
- Jupyter Notebook (교육 자료 구성, 9 Part 구성)
- Markdown (블로그 초안 작성)

## 어려웠던 점 / 해결 방법
- `work()` 호출이 실행이 아니라 코루틴 생성이라는 것 — `type(result)`로 직접 확인해서 해소
- `create_task` 직후 바로 `await`하면 장점이 안 보이는 이유 — 중간에 다른 작업이 있어야 차이가 드러남
- `asyncio.sleep(1)` (await 없음)이 실제로 아무것도 안 한다는 것 — 시간 측정으로 직접 확인

## 결과 / 임팩트
- 코루틴 = 실행 예약표가 아니라 "await해야 진행되는 작업 객체"라는 개념 내재화
- gather vs create_task 차이: 동시 실행 묶음 vs 먼저 시작하고 나중에 결과 수령
- async는 I/O 대기 작업에 강하고 CPU 계산에는 의미 없다는 판단 기준 확립
- 교육 콘텐츠로 재구성 가능한 수준의 이해 도달 → blog draft + ipynb 생성

## 핵심 인사이트 (한 줄)
> await는 "기다린다"가 아니라 "지금 기다리는 동안 다른 작업이 진행될 수 있게 허용하는 신호"다.
