# Daily Log — 2026-03-21

## 오늘 한 일
- 파이썬 데코레이터 작동 원리 학습 (함수를 변수처럼 다루는 개념부터 시작)
- `*args`, `**kwargs` 전체 구조 정리 — 묶기/풀기/혼동 포인트 직접 확인
- 등록형 데코레이터 vs wrapper형 데코레이터 차이 정리
- `*kwargs`(잘못된 문법)와 `**kwargs`(올바른 문법)의 차이 실습으로 확인
- 학습 내용을 blog draft + Jupyter Notebook으로 재구성

## 사용한 기술 / 도구
- Python (decorator, *args, **kwargs, return 흐름)
- Jupyter Notebook (교육 자료 구성)
- Markdown (블로그 초안 작성)

## 어려웠던 점 / 해결 방법
- `test(**kwargs)` 호출 방식(`a=10, b=20`)과 출력 결과(`10 20`)를 구분하는 게 헷갈렸음
  → 호출 방식은 "함수에 값을 넣는 방법"이고, 출력은 print 결과라는 걸 흐름으로 추적해서 해결
- `*kwargs`(별 하나)로 쓰면 키만 전달된다는 것 — 값이 아니라 키가 들어가는 것 직접 확인

## 결과 / 임팩트
- 데코레이터 = 함수 바꿔치기 시스템이라는 핵심 개념 내재화
- 어떤 함수에도 대응하는 표준 wrapper 구조 (`*args, **kwargs` + `return`) 체득
- 자동화 코드에서 데코레이터가 왜 필수인지 실무 연결 완료
- 교육 컨텐츠로 재구성 가능한 수준의 이해 도달 → blog draft + ipynb 생성

## 핵심 인사이트 (한 줄)
> 함수도 값처럼 주고받을 수 있다는 것을 이해한 순간, 데코레이터는 자연스럽게 따라온다.
