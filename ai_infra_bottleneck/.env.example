# .env.example — 실제 값은 .env 파일에 복사해서 넣는다 (절대 커밋 금지)

# 향후 뉴스 API 등 추가 시 사용
# NEWSAPI_KEY=your_key_here
# ALPHA_VANTAGE_KEY=your_key_here

# 프로젝트 루트 경로 (스크립트 자동화 시 사용)
PROJECT_ROOT=/path/to/ai_infra_bottleneck
